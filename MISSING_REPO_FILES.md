# Missing Repo Files Report

Generated during repository fill on 2026-06-25.

## Required Target Structure

All requested target directories now contain tracked, cleaned reproducibility
files:

- `src/tbgmp/`
- `experiments/`
- `configs/`
- `scripts/`
- `data/demo/`
- `data/schema/`
- `results/paper_tables/`
- `results/main_evidence/`
- `results/supporting/`
- `results/boundary_excluded/`
- `figures/paper/`
- `tables/paper/`
- `docs/`
- `slurm/xec/`

## Missing Files

No required target-structure files remain missing.

## Rejected Or Not Imported

- `paper_data_for_writer.zip`: rejected because it is a zip archive.
- `paper_data_for_writer (2).zip`: rejected because it is a zip archive.
- `paper_outputs_from_xec.zip`: rejected because it is a zip archive.
- `paper_outputs_from_xec/gemma2_xec_supporting_evidence_package.zip`:
  rejected because it is a zip archive.
- `paper_data_for_writer/qwen25_zzq/raw_results/*_raw.txt`: rejected because
  raw result logs/full generations are outside the cleaned repository scope.
- `adaptive_validation_results/logs/*.log`: rejected because raw cluster or run
  logs are not uploaded.
- `adaptive_validation_results/qwen25_ablation_logs/*.log`: rejected because raw
  logs are not uploaded.
- `adaptive_validation_results/qwen25_toprisk_policy_logs/*.log`: rejected
  because raw logs are not uploaded.
- `bound_experiments/retrieval_results/*.log`: rejected because raw logs are not
  uploaded.
- `paper_data_for_writer/qwen25_zzq/risk_tables/table_qwen25_full_layer_key_risk.csv`:
  rejected because it contains an unredacted local workstation path.
- HPC `logs/`: rejected because raw cluster logs are not uploaded.
- HPC Slurm files under `[HPC_PROJECT]/slurm/`: not copied
  verbatim because they contain private cluster paths; sanitized equivalents
  were added under `slurm/xec/`.
- `__pycache__/` and `*.pyc`: rejected as generated bytecode.
- Model weights and cache formats (`*.pt`, `*.pth`, `*.bin`, `*.safetensors`,
  `*.ckpt`): excluded by policy and `.gitignore`.

