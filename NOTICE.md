# Notice

This repository uses external model weights and the external TurboQuant runtime
only as user-supplied dependencies. They are not redistributed here.

Users must comply with the respective licenses and access terms of:

- the model checkpoints listed in `configs/model_registry.yaml`;
- the external TurboQuant PyTorch repository;
- PyTorch, Transformers, and other Python dependencies used in their runtime
  environment.

This artifact does not include model weights, raw cluster logs, full raw model
responses, private paths, or production quantizer kernels.
